{% extends 'base.html.twig' %}

{% block body %}
    <h1>Bienvenue {% if(app.user()) %}<b style="color: #1b9448">{{ app.user().getNom() }}</b>{% endif %} chez Escalade H3</h1>
    <p>Consultez nos journée découvertes et participez-y ! </p>
    <h1 style="margin-top:50px;">Comment ça marche ?</h1>
    <p>Veuillez vous connecter ou vous inscrire : Plus vous participez à des journées dévouvertes plus vous gagnez des points.</p>
    {% if(app.user()) %}
        {% if(app.user().getRoles() == "ROLE_OR") %}
            Vous pouvez ainsi créer vos journées découvertes.
        {% elseif (app.user().getRoles() == "ROLE_BRONZE")%}
            Vous pouvez participer à une journée découverte.
        {% elseif (app.user().getRoles() == "ROLE_ARGENT")%}
        {% endif %}
        <br /><br /><br />
        <a href="{{ url('jd.index') }}" class="btn btn-outline-success" style="margin-right: 3px">
            Les journées découverte
        </a>
    {% else %}
        <a href="{{ path('app_login') }}" class="btn btn-success">Se connecter</a>
        <a href="{{ path('app_register') }}" class="btn btn-secondary">S'inscrire</a>
    {% endif %}
{% endblock %}